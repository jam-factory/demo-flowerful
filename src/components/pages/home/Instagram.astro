---
import Container from "@/components/container/Container.astro";
import SectionTitle from "@/components/section-title/SectionTitle.astro";
import InstagramPosts from '@/components/instagram-posts/InstagramPosts.jsx';
import { FaInstagram } from "react-icons/fa";
---

<section class="section">
  <Container>
    <h2 class="title">
      <SectionTitle
        en="Instagram"
        jp="インスタグラム"
      />
    </h2>
    <p class="intro">
      最新のフラワーアレンジメントや店内の様子をInstagramでシェアしています。
      <br />
      季節の花やイベント情報、特別なプロモーションなど、
      <br />
      お得な最新情報をお届けしますので、ぜひフォローしてください。
    </p>
    <div class="list">
      <InstagramPosts client:idle />
    </div>

    <div class="btn-wrap">
      <a
        href="https://www.instagram.com/jam_factory.demo02/"
        target="_blank"
        rel="noopener noreferrer"
        class="btn"
      >
        <span>Instagram</span>
        <FaInstagram />
      </a>
    </div>

  </Container>
</section>

<style lang="scss">
  .section {
    background: var.$c-white;
    padding: 16rem 0;

    @include mixin.sp {
      padding: 8rem 0;
    }
  }

  .title {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .intro {
    text-align: center;
    font-family: var.$font-zen;
    font-size: 1.5rem;
    font-weight: 500;
    line-height: 2.25;
    margin-top: 4rem;

    @include mixin.sp {
      font-size: 1.3rem;
      margin-top: 2.4rem;
    }

    @include mixin.sp-s {
      text-align: left;

      br {
        display: none;
      }
    }
  }

  .list {
    margin-top: 4.8rem;

    @include mixin.sp {
      margin-top: 2.4rem;
    }
  }

  .btn-wrap {
    display: flex;
    justify-content: center;
    margin-top: 6.4rem;

    @include mixin.sp {
      margin-top: 3.2rem;
    }
  }

  .btn {
    position: relative;
    width: 24rem;
    height: 6rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2.4rem;
    border: 0.1rem solid var.$c-black;
    background-color: var.$c-white;
    z-index: 1;
    transition: all 0.4s;

    @include mixin.sp {
      width: 20rem;
      height: 4.8rem;
      padding: 0 1.6rem;
    }

    span {
      position: relative;

      font-family: var.$font-base;
      font-weight: 600;
      font-size: 1.5rem;
      letter-spacing: 0.04em;

      @include mixin.sp {
        font-size: 1.2rem;
        letter-spacing: 0.02em;
      }
    }

    svg {
      font-size: 2.2rem;

      @include mixin.sp {
        font-size: 2rem;
      }
    }

    @include mixin.hover {
      &:hover {
        color: var.$c-white;
        background-color: var.$c-black;
      }
    }
  }
</style>